sourceSets { 
	 main { 
	 	java{
	 		srcDir "$projectDir/src"
	 	}
	 	resources{
		 	srcDir "$projectDir/site-src"
		 }
	 } 
 	}
 	sourceCompatibility = 1.8
    targetCompatibility = 1.8
 	tasks.withType(JavaCompile) {  
        options.encoding = "UTF-8"  
    } 
 repositories { 
 } 

dependencies {
    compile group: 'cj.netos.openport', name: 'cj.studio.openport', version: ref_cj_studio_openport_version
    compile group: 'cj.netos.rabbitmq', name: 'cj.netos.rabbitmq.ultimate', version: ref_cj_netos_rabbitmq_ultimate_version
    compile group: 'cj.netos.rabbitmq', name: 'cj.netos.rabbitmq.producer', version: ref_cj_netos_rabbitmq_producer_version
}

 task releaseToProgram(type: Copy) {
 dependsOn "build"
 from 'build/libs/'
 into '../cj.netos.gateway.wybank.program/src/cj/references'
 include '**/*.jar'
 exclude '**/*-javadoc.jar','**/*-sources.jar'
 }
 task releaseToOutputs(type: Copy) {
 dependsOn "build"
 from 'build/libs/'
 into '../outputs'
 include '**/*.jar'
 }
 task release(type:Copy){
  dependsOn "releaseToProgram","releaseToOutputs"
 }
